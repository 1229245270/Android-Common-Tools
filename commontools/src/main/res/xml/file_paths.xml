<paths>
    <!--Context.getFilesDir()  示例 : /data/data/com.chen.gradle/files/test1-->
    <files-path
        name="files_path"
        path="."/>

    <!--表示Context.getCacheDir()目录或者其子目录。示例 : /data/data/com.chen.gradle/cache/test2-->
    <cache-path
        name="cache_path"
        path="."/>

    <!--表示Environment.getExternalStorageDirectory()目录或者其子目录。-->
    <external-path
        name="external_path"
        path="."/>

    <external-path
        name="external_storage_root"
        path="." />

    <!--表示Context.getExternalFilesDir(null)目录或者其子目录。示例 : /storage/emulated/0/Android/data/com.chen.gradle/files/test4-->
    <external-files-path
        name="external_files_path"
        path="."/>

    <external-cache-path
        name="external_cache_path"
        path="." />

    <!--配置root-path。这样子可以读取到sd卡和一些应用分身的目录，否则微信分身保存的图片，就会导致 java.lang.IllegalArgumentException: Failed to find configured root that contains /storage/emulated/999/tencent/MicroMsg/WeiXin/export1544062754693.jpg，在小米6的手机上微信分身有这个crash，华为没有
    -->
    <root-path
        name="root-path"
        path="" />
</paths>